# TODOS

Deferred work: accepted as worth doing, deliberately not in current scope. Not a bug list
(see the private `mukund1312/mtdo-bugs` tracker for that) and not a backlog of ideas —
only things a plan or review explicitly pushed out.

## Group-study dashboard (`docs/designs/mtdo-group-study-dashboard-plan.md`)

- **"I'm stuck" help lane (D8).** Structured help-request flow inside the Room: a short
  "stuck on X" request visible to the squad (and optionally the AI manager), with
  lightweight replies and a visible resolved/unresolved state — deliberately not generic
  chat. Deferred out of v1 scope in the `/plan-ceo-review` session on 2026-09-03; full
  chat was rejected outright (not deferred).
  **Revisit when:** Phase 1 of the room feature has shipped and real groups have used it
  for at least one exam/goal cycle. Check whether users are asking each other for help
  in-product already (weak signal it's needed) or leaving the room to solve problems
  elsewhere (stronger signal it's needed, since the group habit is leaking outside the
  product).
